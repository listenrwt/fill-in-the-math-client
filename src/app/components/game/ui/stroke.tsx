import { Divider } from '@mui/material';

export default function GameContainer() {
  return (
    <Divider
      sx={{
        borderBottomWidth: 1.5,
        borderColor: '#000000',
        borderRadius: 1,
        width: '90%',
        alignContent: 'center',
        margin: '0 auto',
      }}
    />
  );
}
